import React from "react";
const RedesignLabel = (props) => {
  return (
    <article>
      <h2>{props.name}</h2>
      <p>{props.description}</p>
    </article>
  );
};

export default RedesignLabel;
